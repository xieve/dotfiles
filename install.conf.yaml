- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.tmux.conf: tmux.conf
    ~/.tmuxline.theme: tmuxline.theme
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
    ~/.vim: vim

- shell:
  - [git submodule update --init --recursive, Installing submodules]
